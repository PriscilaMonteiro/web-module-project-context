import { createContext } from "react";

const ProductContext = createContext()

export default ProductContext

// when creating context it is better not to capitalize the name of the file to be clear that it is not a component file.

